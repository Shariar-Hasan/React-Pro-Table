import React from "react";
declare const Checkbox: ({ item, checkAll, onClick, }: {
    item?: any;
    checkAll?: boolean;
    onClick?: () => void;
}) => React.JSX.Element;
export default Checkbox;
